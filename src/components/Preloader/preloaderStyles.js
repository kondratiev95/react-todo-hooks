import { makeStyles } from "@mui/styles";

export const preloaderStyles = makeStyles({
  preloader: {
    display: "block",
    margin: "0px auto",
    color: "rosybrown",
    width: "50px",
    height: "50px",
    fontSize: "50px",
  },
});
