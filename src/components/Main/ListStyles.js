import { makeStyles } from "@mui/styles";

export const ListStyles = makeStyles({
  todoList: {
    margin: "0px auto",
    listStyleType: "none",
  },
  todoError: {
    textAlign: "center",
    fontWeight: "normal",
    padding: "10px",
    fontStyle: "italic",
    color: "tomato",
  },
});
