export const filterTypeSelector = state => state.todoReducer.type;

export const getTodosSelector = state => state.todoReducer.todos;

export const getErrorSelector = state => state.todoReducer.error;